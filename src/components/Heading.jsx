import React from 'react';
import './style.css'
const Heading = () => {
    return (
        <div>
            <h2 className="head">Expense tracker</h2>
        </div>
    );
};

export default Heading;